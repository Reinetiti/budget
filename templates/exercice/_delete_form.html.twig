<form method="post" action="{{ path('exercice_delete', {'annee': exercice.annee}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice.annee) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
