<form method="post" action="{{ path('departement_delete', {'depid': departement.depid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departement.depid) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
