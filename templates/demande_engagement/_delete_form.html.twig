<form method="post" action="{{ path('demande_engagement_delete', {'numDemande': demande_engagement.numDemande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_engagement.numDemande) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
