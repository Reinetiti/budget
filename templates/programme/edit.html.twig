{% extends 'base.html.twig' %}

{% block title %}Modifier le Programme{% endblock %}

{% block body %}
    <h1>Modifier le Programme</h1>
<div class="card p-3 align-items-center">
    {{ include('programme/_form.html.twig', {'button_label': 'Mise à jour'}) }}<br>

    <a href="{{ path('programme_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a><br>

    {{ include('programme/_delete_form.html.twig') }}
</div>
{% endblock %}
