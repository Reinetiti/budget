<form method="post" action="{{ path('rubrique_delete', {'rubriqueid': rubrique.rubriqueid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rubrique.rubriqueid) }}">
    <button class="btn">Supprimer</button>
</form>
