{% extends 'base.html.twig' %}

{% block title %}Modifier la Rubrique{% endblock %}

{% block body %}
    <h1>Modifier la Rubrique</h1>
<div class="card p-3 align-items-center">
    {{ include('rubrique/_form.html.twig', {'button_label': 'Mise à jour'}) }}<br>

    <a href="{{ path('rubrique_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a><br>

    {{ include('rubrique/_delete_form.html.twig') }}
</div>
{% endblock %}
