<form method="post" action="{{ path('engagement_delete', {'numEngagement': engagement.numEngagement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ engagement.numEngagement) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
