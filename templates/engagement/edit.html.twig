{% extends 'base.html.twig' %}

{% block title %}Modifier l'Engagement{% endblock %}

{% block body %}
    <h1>Modifier l'Engagement</h1>
<div class="card p-3 align-items-center">
    {{ include('engagement/_form.html.twig', {'button_label': 'Mise à jour'}) }}<br>

    <a href="{{ path('engagement_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a><br>

    {{ include('engagement/_delete_form.html.twig') }}
</div>
{% endblock %}
