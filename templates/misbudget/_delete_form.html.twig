<form method="post" action="{{ path('misbudget_delete', {'misbudgetid': misbudget.misbudgetid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ misbudget.misbudgetid) }}">
    <button class="btn">Supprimer</button>
</form>
