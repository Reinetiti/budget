<form method="post" action="{{ path('typeunite_delete', {'typeid': typeunite.typeid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeunite.typeid) }}">
    <button class="btn">Supprimer</button>
</form>
