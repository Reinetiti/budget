<form method="post" action="{{ path('mandat_delete', {'numMandat': mandat.numMandat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mandat.numMandat) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
