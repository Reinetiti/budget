{% extends 'base.html.twig' %}

{% block title %}Editer l'action{% endblock %}

{% block body %}
<div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h1>Modification de l'action</h1>
            </div>
        </div>

    {# <h1>Editer l'action</h1> #}
    {{ include('action/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <a href="{{ path('action_index') }}" class="btn">retour à la liste</a>

    {{ include('action/_delete_form.html.twig') }}
</div>
{% endblock %}

