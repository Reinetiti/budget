<form method="post" action="{{ path('uniteadmin_delete', {'uniteid': uniteadmin.uniteid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ uniteadmin.uniteid) }}">
    <button class="btn">Supprimer</button>
</form>
