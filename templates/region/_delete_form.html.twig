<form method="post" action="{{ path('region_delete', {'regionid': region.regionid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ region.regionid) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
