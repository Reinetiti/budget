{% extends 'base.html.twig' %}

{% block title %}Modifier la Paragraphe{% endblock %}

{% block body %}
    <h1>Modifier la Paragraphe</h1>
<div class="card p-3 align-items-center">
    {{ include('paragraphe/_form.html.twig', {'button_label': 'Mise à jour'}) }}<br>

    <a href="{{ path('paragraphe_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a><br>

    {{ include('paragraphe/_delete_form.html.twig') }}
</div>
{% endblock %}
