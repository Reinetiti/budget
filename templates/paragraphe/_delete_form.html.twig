<form method="post" action="{{ path('paragraphe_delete', {'paragrapheid': paragraphe.paragrapheid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paragraphe.paragrapheid) }}">
    <button class="btn">Supprimer</button>
</form>
