<form method="post" action="{{ path('groupe_delete', {'groupeid': groupe.groupeid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupe.groupeid) }}">
    <button class="btn">Supprimer</button>
</form>
