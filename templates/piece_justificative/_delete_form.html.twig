<form method="post" action="{{ path('piece_justificative_delete', {'idPiece': piece_justificative.idPiece}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece_justificative.idPiece) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
