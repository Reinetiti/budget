<form method="post" action="{{ path('classe_delete', {'classeid': classe.classeid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.classeid) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
