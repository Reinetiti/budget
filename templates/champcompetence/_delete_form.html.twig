<form method="post" action="{{ path('champcompetence_delete', {'competenceid': champcompetence.competenceid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ champcompetence.competenceid) }}">
    <button class="btn">Supprimer</button>
</form>
