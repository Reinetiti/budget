{% extends 'base.html.twig' %}

{% block title %}Editer l'article{% endblock %}

{% block body %}
    <h1 class="text-center">Modifier l'article</h1>
    <div class="card p-3 align-items-center">
        {{ include('article/_form.html.twig', {'button_label': 'Mise à jour'}) }}<br>

        <a href="{{ path('article_index') }}" class="btn btn-secondary btn-sm">Retour à la liste</a><br>

        {{ include('article/_delete_form.html.twig') }}
    </div>
{% endblock %}
