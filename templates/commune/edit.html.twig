{% extends 'base.html.twig' %}

{% block title %}Modifier la Commune{% endblock %}

{% block body %}
    <h1>Modifier la Commune</h1>
<div class="card p-3 align-items-center">
    {{ include('commune/_form.html.twig', {'button_label': 'Mise à jour'}) }}<br>

    <a href="{{ path('commune_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a><br>

    {{ include('commune/_delete_form.html.twig') }}
</div>
{% endblock %}
