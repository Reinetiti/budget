{% extends 'base.html.twig' %}

{% block title %}Modifier la Commune{% endblock %}

{% block body %}
    <h1>Modifier la Commune</h1>

    {{ include('commune/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <a href="{{ path('commune_index') }}">retour à la liste</a>

    {{ include('commune/_delete_form.html.twig') }}
{% endblock %}
