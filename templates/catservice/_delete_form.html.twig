<form method="post" action="{{ path('catservice_delete', {'catid': catservice.catid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ catservice.catid) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
