<form method="post" action="{{ path('budgetisation_delete', {'id': budgetisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budgetisation.id) }}">
    <button class="btn">Supprimer</button>
</form>
